
# docker compose down
docker compose down
docker stop boat_nginx

# docker compose up
docker compose -f docker-compose.prod.yml up -d
docker compose --file docker-compose.prod.yml up --detach
## with build
docker compose -f docker-compose.prod.yml up -d --build
